<h1>Listing product_filters</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @product_filters.each do |product_filter| %>
  <tr>
    <td><%= link_to 'Show', product_filter %></td>
    <td><%= link_to 'Edit', edit_product_filter_path(product_filter) %></td>
    <td><%= link_to 'Destroy', product_filter, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product filter', new_product_filter_path %>
